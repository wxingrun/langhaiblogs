diff --git a/README.md b/README.md
index 7b52a00..72d33d1 100644
--- a/README.md
+++ b/README.md
@@ -18,6 +18,14 @@
 linux ==>> nohup java -jar langhai-blogs.jar > langhai.log &  
 windows ==>> java -jar langhai-blogs.jar
 
+Docker 一键启动：
+1. 在项目根目录执行 `docker-compose up -d`，首次启动会自动构建应用镜像并启动 MySQL、Redis、MinIO 与博客服务。
+2. 启动完成后访问 `http://localhost:2086` 即可查看博客首页。
+3. MinIO 控制台地址为 `http://localhost:9001`，默认账号密码均为 `minioadmin`。
+4. MySQL 数据持久化目录为 `./docker/mysql/data`，Redis 数据持久化目录为 `./docker/redis/data`，MinIO 数据持久化目录为 `./docker/minio/data`。
+5. 首次初始化会自动执行 `sql/langhaiblogs.sql`；如果已经存在 MySQL 数据目录，初始化脚本不会重复执行。
+6. 停止服务执行 `docker-compose down`，保留数据；如需连同容器一起重建，可先删除 `./docker` 目录后重新启动。
+
 技术选型：  
 springboot 后端快速构建框架  
 thymeleaf 数据模板引擎  
@@ -50,4 +58,4 @@ INSERT INTO role VALUES(3, 'vip', NOW(), NULL);
 非关系型数据库 Redis  
 图片存储服务器 minio [minio 详细说明](http://www.langhai.cc/article/articleShow?id=54)   
 搜索引擎(可选) elasticSearch [elasticSearch 相关说明](http://www.langhai.cc/article/articleShow?id=55)   
-消息队列（可选）rabbitMQ   <a href="https://langhai.cc/article/articleShow?id=33">rabbitMQ所有说明</a>
\ No newline at end of file
+消息队列（可选）rabbitMQ   <a href="https://langhai.cc/article/articleShow?id=33">rabbitMQ所有说明</a>
diff --git a/src/main/resources/application.yml b/src/main/resources/application.yml
index 9930ec8..bcf9536 100644
--- a/src/main/resources/application.yml
+++ b/src/main/resources/application.yml
@@ -24,13 +24,13 @@ spring:
   # 邮件发送配置
   mail:
     # 配置SMTP的服务器地址
-    host: smtp.qq.com
+    host: ${MAIL_HOST:smtp.qq.com}
     # 发送者的QQ邮箱
-    username:
+    username: ${MAIL_USERNAME:}
     # 配置秘钥，授权码（不是QQ密码）
-    password:
+    password: ${MAIL_PASSWORD:}
     # 端口号配置
-    port: 587
+    port: ${MAIL_PORT:587}
     # 邮件的编码格式
     default-encoding: utf-8
     # 配置SSL加密
@@ -42,17 +42,17 @@ spring:
 
   # redis配置
   redis:
-    database: 9
-    host:
-    password:
-    timeout: 20000
-    port: 6379
+    database: ${REDIS_DATABASE:9}
+    host: ${REDIS_HOST:redis}
+    password: ${REDIS_PASSWORD:}
+    timeout: ${REDIS_TIMEOUT:20000}
+    port: ${REDIS_PORT:6379}
 
   # mysql数据库配置
   datasource:
-    url: jdbc:mysql://x.x.x.x:3306/langhaiblogs?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
-    username:
-    password:
+    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:langhaiblogs}?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
+    username: ${MYSQL_USERNAME:}
+    password: ${MYSQL_PASSWORD:}
     driver-class-name: com.mysql.cj.jdbc.Driver
     type: com.alibaba.druid.pool.DruidDataSource
 
@@ -62,11 +62,11 @@ spring:
       max-request-size: 50MB
 
   rabbitmq:
-    host: 127.0.0.1 # 主机名
-    port: 5672 # 端口
-    virtual-host: / # 虚拟主机
-    username: xxx # 用户名
-    password: xxx # 密码
+    host: ${RABBITMQ_HOST:127.0.0.1} # 主机名
+    port: ${RABBITMQ_PORT:5672} # 端口
+    virtual-host: ${RABBITMQ_VIRTUAL_HOST:/} # 虚拟主机
+    username: ${RABBITMQ_USERNAME:xxx} # 用户名
+    password: ${RABBITMQ_PASSWORD:xxx} # 密码
 
 #  profiles:
 #    active: test
@@ -77,16 +77,16 @@ pagehelper:
 
 # minio文件存储配置信息
 minio:
-  endpoint: http://127.0.0.1:9000
-  accessKey: minioadmin
-  secretKey: minioadmin
+  endpoint: ${MINIO_ENDPOINT:http://minio:9000}
+  accessKey: ${MINIO_ACCESS_KEY:minioadmin}
+  secretKey: ${MINIO_SECRET_KEY:minioadmin}
 
 # 配置es相关信息
 es:
-  ip: 127.0.0.1
-  port: 9200
-  userName: xxx
-  password: xxx
+  ip: ${ES_HOST:127.0.0.1}
+  port: ${ES_PORT:9200}
+  userName: ${ES_USERNAME:xxx}
+  password: ${ES_PASSWORD:xxx}
 
 logging:
   level:
@@ -94,9 +94,9 @@ logging:
 
 netty:
   # 监听websocket连接的端口
-  port: 20202
+  port: ${NETTY_PORT:20202}
   # websocket连接地址
-  ws: ws://127.0.0.1:${netty.port}/ws
+  ws: ws://${NETTY_WS_HOST:127.0.0.1}:${netty.port}/ws
 
 #flowable:
 #  standalone:
@@ -107,4 +107,4 @@ netty:
 #    enabled: false # 检测身份信息
 #  database-schema-update: true # 没有数据库表的时候生成数据库表
 #  process-definition-location-prefix: classpath*:/flowable/
-#  process-definition-location-suffixes: "**.bpmn20.xml, **.bpmn"
\ No newline at end of file
+#  process-definition-location-suffixes: "**.bpmn20.xml, **.bpmn"

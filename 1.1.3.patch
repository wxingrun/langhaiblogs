diff --git a/src/main/resources/application.yml b/src/main/resources/application.yml
index 9930ec8..9e741a0 100644
--- a/src/main/resources/application.yml
+++ b/src/main/resources/application.yml
@@ -43,16 +43,16 @@ spring:
   # redis配置
   redis:
     database: 9
-    host:
-    password:
+    host: ${REDIS_HOST:redis}
+    password: ${REDIS_PASSWORD:}
     timeout: 20000
-    port: 6379
+    port: ${REDIS_PORT:6379}
 
   # mysql数据库配置
   datasource:
-    url: jdbc:mysql://x.x.x.x:3306/langhaiblogs?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
-    username:
-    password:
+    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/langhaiblogs?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
+    username: ${MYSQL_USER:root}
+    password: ${MYSQL_PASSWORD:langhai123}
     driver-class-name: com.mysql.cj.jdbc.Driver
     type: com.alibaba.druid.pool.DruidDataSource
 
@@ -62,11 +62,11 @@ spring:
       max-request-size: 50MB
 
   rabbitmq:
-    host: 127.0.0.1 # 主机名
-    port: 5672 # 端口
+    host: ${RABBITMQ_HOST:rabbitmq} # 主机名
+    port: ${RABBITMQ_PORT:5672} # 端口
     virtual-host: / # 虚拟主机
-    username: xxx # 用户名
-    password: xxx # 密码
+    username: ${RABBITMQ_USER:langhai} # 用户名
+    password: ${RABBITMQ_PASS:langhai123} # 密码
 
 #  profiles:
 #    active: test
@@ -77,16 +77,16 @@ pagehelper:
 
 # minio文件存储配置信息
 minio:
-  endpoint: http://127.0.0.1:9000
-  accessKey: minioadmin
-  secretKey: minioadmin
+  endpoint: ${MINIO_ENDPOINT:http://minio:9000}
+  accessKey: ${MINIO_ACCESS_KEY:minioadmin}
+  secretKey: ${MINIO_SECRET_KEY:minioadmin}
 
 # 配置es相关信息
 es:
-  ip: 127.0.0.1
-  port: 9200
-  userName: xxx
-  password: xxx
+  ip: ${ES_HOST:127.0.0.1}
+  port: ${ES_PORT:9200}
+  userName: ${ES_USER:xxx}
+  password: ${ES_PASS:xxx}
 
 logging:
   level:
@@ -96,7 +96,7 @@ netty:
   # 监听websocket连接的端口
   port: 20202
   # websocket连接地址
-  ws: ws://127.0.0.1:${netty.port}/ws
+  ws: ws://${NETTY_WS_HOST:127.0.0.1}:${netty.port}/ws
 
 #flowable:
 #  standalone:

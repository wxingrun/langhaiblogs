diff --git a/src/main/resources/application.yml b/src/main/resources/application.yml
index 9930ec8..86d774c 100644
--- a/src/main/resources/application.yml
+++ b/src/main/resources/application.yml
@@ -43,16 +43,16 @@ spring:
   # redis配置
   redis:
     database: 9
-    host:
-    password:
+    host: ${SPRING_REDIS_HOST:redis}
+    password: ${SPRING_REDIS_PASSWORD:}
     timeout: 20000
-    port: 6379
+    port: ${SPRING_REDIS_PORT:6379}
 
   # mysql数据库配置
   datasource:
-    url: jdbc:mysql://x.x.x.x:3306/langhaiblogs?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
-    username:
-    password:
+    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/langhaiblogs?userUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8}
+    username: ${SPRING_DATASOURCE_USERNAME:root}
+    password: ${SPRING_DATASOURCE_PASSWORD:root123}
     driver-class-name: com.mysql.cj.jdbc.Driver
     type: com.alibaba.druid.pool.DruidDataSource
 
@@ -77,9 +77,9 @@ pagehelper:
 
 # minio文件存储配置信息
 minio:
-  endpoint: http://127.0.0.1:9000
-  accessKey: minioadmin
-  secretKey: minioadmin
+  endpoint: ${MINIO_ENDPOINT:http://minio:9000}
+  accessKey: ${MINIO_ACCESS_KEY:minioadmin}
+  secretKey: ${MINIO_SECRET_KEY:minioadmin}
 
 # 配置es相关信息
 es:
